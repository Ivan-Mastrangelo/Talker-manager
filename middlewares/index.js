const validToken = require('./validToken');
const nameAndAgeValid = require('./nameAndAgeValid');
const validDate = require('./validDate');
const validRate = require('./validRate');

module.exports = {
  validToken,
  nameAndAgeValid,
  validDate,
  validRate,
};
